namespace UI
{
    internal class Program
    {
        static void Main(string[] args)
        {
           //UserWorker worker = new UserWorker();
           // worker.StartWork();
           var a =double.Parse(Console.ReadLine());
           Variable variable=new Variable("daa",a);
        }
       
    }
}